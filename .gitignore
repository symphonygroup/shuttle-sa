# AI SDD
CURRENT.md

# Dependencies
node_modules/

# Playwright MCP
.playwright-mcp/
.playwright-cli/
.playwright/

# Environment (secrets — Railway reads env vars from its dashboard, not this file)
.env
.env.local
.env.*.local

# Session files (dev only)
data/sessions/

# Runtime DB (contains user data + push tokens — seeded from data/db.example.json)
data/db.json
data/db.json.tmp

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V1
.Trashes

# Logs
logs/
*.log
npm-debug.log*

# Runtime
*.pid
